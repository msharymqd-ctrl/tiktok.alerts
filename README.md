# Taskflow To-do App

A responsive to-do list application with local storage. Add tasks, mark them complete, delete them, filter them, and clear completed tasks. Open `index.html` in a browser to use it; tasks persist on the same browser and device.
